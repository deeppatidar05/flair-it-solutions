import { FilterNameStartwithPipe } from './filter-name-startwith.pipe';

describe('FilterNameStartwithPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameStartwithPipe();
    expect(pipe).toBeTruthy();
  });
});
